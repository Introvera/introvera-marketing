/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://introvera.com',
  generateRobotsTxt: true,
  generateIndexSitemap: false, // only needed if you're not using multiple sitemaps
};
